# 2026-03-21

## Sidebar 按 HIG 重构（00:16）
- 用户要求按 Apple HIG `sidebars` 规范重设计侧边栏。
- 通过 Apple 文档数据接口确认关键约束：侧栏层级控制、系统符号与可发现性、运行控制位置语义。
- 先在 `docs-dev/features/apple-native-uiux-redesign-2026-03-20.md` 增加 HIG 场景验收条目，再进入代码改造。
- TDD：
  - 新增 `Tests/EnjoyableKitTests/SidebarHierarchyPresentationTests.swift`，先红灯（`DeviceRuntimeSidebarHierarchy` 缺失）。
  - 实现 `DeviceRuntimeSidebarHierarchy.project(_:)` 后转绿。
- 代码实现：
  - `EnjoyableRootComponents.swift`
    - 新增设备树两级投影器 `DeviceRuntimeSidebarHierarchy`。
    - 侧栏改为单一 `List(.sidebar)`，分为 `Runtime` 与 `Devices` 两个 section。
    - 运行开关和辅助功能权限提示移入列表上部，避免固定到底部工具区。
  - `EnjoyableRootView.swift`
    - 去除根导航外层 padding，恢复更原生的侧栏高度与边界呈现。
  - 多语言文案新增 `runtime_title`（中/英）。
- 验证：
  - `swift test --filter SidebarHierarchyPresentationTests` 通过。
  - `swift test` 全量通过（48 tests, 0 failed）。
  - `xcodebuild ... test -only-testing:EnjoyableTests` 默认失败（部署目标不一致），使用 `MACOSX_DEPLOYMENT_TARGET=26.2` 覆盖后通过。

## open accessibility settings 文档化修正（00:37）
- 用户要求按 `xcdocs` 指南修正“打开辅助功能设置”实现。
- 使用 `xcdocs` 检索并确认：
  - `AXIsProcessTrustedWithOptions(_:)`
  - `kAXTrustedCheckOptionPrompt`
  - 文档语义为“异步提示用户完成授权”。
- 代码改动：
  - `openAccessibilitySettings()` 从 URL Scheme 跳转改为 `AXIsProcessTrustedWithOptions` 提示流。
  - 新增 `AccessibilityPermissionNavigator` 封装 prompt options 与调用。
- 测试：
  - 新增 `AccessibilityPermissionNavigatorTests`，验证 prompt options 字典包含 `kAXTrustedCheckOptionPrompt: true`。
- 回归：
  - `swift test --filter AccessibilityPermissionNavigatorTests` 通过。
  - `swift test` 通过（49 tests, 0 failed）。
  - `xcodebuild ... MACOSX_DEPLOYMENT_TARGET=26.2 test -only-testing:EnjoyableTests` 通过。

## 侧栏样式二次收敛（00:53）
- 用户追加反馈“样式是否需要调整”，继续收敛侧栏视觉语义。
- 代码改动：
  - `EnjoyableRootComponents.swift`
    - 权限提示区改为系统告警语义：`exclamationmark.triangle.fill` + 黄色强调。
    - “打开辅助功能设置”按钮改为 `Label + .bordered + .small`，符号 `arrow.up.right.square`。
    - 设备树图标前景改为 `secondary/tertiary`，减弱非原生高饱和强调。
  - `EnjoyableRuntime.swift`
    - `DeviceTreeNodeStyle.symbolName(for:)` 更新为：
      - 设备：`gamecontroller`
      - 轴：`dial.horizontal`
      - 按键：`button.horizontal`
      - 叶子：`smallcircle.filled.circle`
  - `EnjoyableRootComponents+Preview.swift`
    - 两个侧栏预览统一高度为 `760`，确保预览比例接近真实 Sidebar。
- 测试：
  - 更新 `DeviceTreeNodeStyleTests` 断言后通过。
  - `swift test --filter DeviceTreeNodeStyleTests` 通过。
  - `swift test` 全量通过（49 tests, 0 failed）。

## 按键详情页二次重构（01:56）
- 用户明确范围为“按键详情页”，要求彻底重构交互。
- 需求文档先行更新：`docs-dev/features/key-mapping-editor-interaction-refresh-2026-03-21.md` 增加二次验收（单路径编辑流、保存规则、宏空态文案）。
- TDD：
  - `KeyMappingEditorStateTests` 先新增红灯断言：
    - `canSaveChanges`
    - `hasConfiguredMacroStep`
  - 首次运行编译失败（缺少属性）后再补实现转绿。
- 代码重构：
  - `AppleKeyMappingEditorSheet.swift`
    - 由 `HSplitView` 双栏改为单栏 `Form` 编辑流。
    - 顶部增加“未保存”状态标记。
    - 启用但无有效按键时显示提示，并禁用保存。
    - 宏区空态改为专用提示文案。
  - `EnjoyableRuntime.swift`
    - `KeyMappingEditorState` 新增：
      - `hasConfiguredMacroStep`
      - `resolvedPrimaryKeyCode`
      - `canSaveChanges`
    - `applyKeyMappingEditorState(_:)` 使用 `resolvedPrimaryKeyCode`，保证 UI 保存判定与应用逻辑一致。
  - 本地化新增：
    - `editor_unsaved_changes`
    - `editor_binding_required_hint`
    - `macro_sequence_empty_hint`
- 回归：
  - `swift test --filter KeyMappingEditorStateTests` 通过。
  - `swift test` 全量通过（57 tests, 0 failed）。

## 手柄区无障碍语义补强（01:01）
- 用户要求继续修改手柄区并按 `xcdocs` 对齐 Apple 文档实践。
- 使用 `xcdocs` 检索并确认 SwiftUI 无障碍修饰符：
  - `/documentation/SwiftUI/View-Accessibility`
  - `/documentation/SwiftUI/Accessible-descriptions#Applying-labels`
- 代码改动：
  - `GamepadControl` 新增 `accessibilityName`、`accessibilityLabel(controlPrefix:)`、`accessibilityIdentifier`。
  - `GamepadLayoutView` 的每个手柄按钮新增：
    - `.accessibilityLabel(...)`
    - `.accessibilityHint(...)`
    - `.accessibilityIdentifier(...)`
- 测试：
  - `GamepadLayoutTests` 新增无障碍标签与 identifier 稳定性断言。
  - `swift test --filter GamepadLayoutTests` 通过（7 tests, 0 failed）。
- 文档：
  - `docs-dev/features/stitch-advanced-editor-and-svg-dashboard-2026-03-20.md` 新增“场景 5：手柄区无障碍语义”。

## 手柄区玻璃覆盖修正（01:13）
- 用户反馈手柄区出现“玻璃层盖住区域”问题，影响内容可见性。
- 决策：保留全局玻璃风格，但将手柄区卡片改为实体背景，避免主交互区被玻璃压暗。
- 代码改动：
  - 新增 `NativeCardSurfaceStyle`（`.adaptiveGlass` / `.solid`）。
  - `AppleNativeCard` 增加 `surfaceStyle` 参数。
  - `EnjoyableRootView` 增加 `RootCardSurfacePolicy.controllerCardStyle = .solid`，并用于手柄区卡片。
- 测试：
  - `AppleNativeDesignTests` 新增 `testControllerCardUsesSolidSurfaceStyle`。
  - `swift test --filter AppleNativeDesignTests` 通过（5 tests, 0 failed）。

## 剩余三块玻璃区移除（01:20）
- 用户继续反馈“还有 3 大玻璃区”。
- 决策：将核心编辑路径的 4 张卡片统一改为实体背景，避免玻璃层影响可读性：
  - 手柄区（controller）
  - 实时轴数据（live axes）
  - 映射管理（mappings）
  - 输出编辑（output editor）
- 代码改动：
  - `RootCardSurfacePolicy` 新增 `liveAxisCardStyle`、`mappingManagerCardStyle`、`outputEditorCardStyle`，均为 `.solid`。
  - `EnjoyableRootView` 对映射管理卡与输出编辑卡传入实体样式。
  - `EnjoyableLiveAxisCardView` 增加 `surfaceStyle` 参数并在主页面传入实体样式。
- 测试：
  - 更新 `AppleNativeDesignTests.testControllerCardUsesSolidSurfaceStyle`，覆盖 4 张卡片策略。
  - `swift test --filter AppleNativeDesignTests` 通过（5 tests, 0 failed）。
  - `swift test --filter GamepadLayoutTests` 通过（7 tests, 0 failed）。

## 按键详情页交互重构（01:44）
- 用户要求按 `$xcdocs` 与交互指南重构 `AppleKeyMappingEditorSheet`。
- 关键决策：
  - 按“源输入/诊断 + 编辑配置”拆分为 `HSplitView` 双栏，降低单列表单信息密度。
  - 对取消行为增加未保存变更确认，减少误操作导致的数据丢失。
  - 将可编辑控件门禁统一下沉到 `KeyMappingEditorState` 计算属性，便于测试与维护。
- 代码改动：
  - `AppleKeyMappingEditorSheet.swift`：双栏布局、取消确认弹窗、不可解析时禁用保存、表单可编辑门禁统一。
  - `EnjoyableRuntime.swift`：新增 `hasSameEditingPayload(as:)`、`canEditBindingControls`、`canRemoveMacroStep`。
  - `Localizable.strings`（中英）：新增诊断区与放弃更改提示文案。
- 测试与验证：
  - 先红灯：`KeyMappingEditorStateTests` 新增交互状态测试后失败（缺少实现）。
  - 后绿灯：补实现后 `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - 全量回归：`swift test -q` 通过（55 tests, 0 failed）。
- 文档更新：
  - `docs-dev/features/key-mapping-editor-interaction-refresh-2026-03-21.md`
  - `docs-dev/dev/key-mapping-editor-interaction-refresh-implementation-2026-03-21.md`

## 按键详情页布局二次收敛（02:00）
- 用户确认继续收敛交互细节。
- 调整内容：
  - 左栏固定宽度（280）承载源输入与诊断。
  - 右栏编辑区自适应拉伸。
  - 编辑项采用统一标签列宽（118）与 `Label:` 风格，提升对齐与扫描效率。
  - 宏步骤文案键新增 `macro_step_prefix`，步骤行由 `#N` 改为语义化“步骤 N / Step N”。
- 验证：`swift test --filter KeyMappingEditorStateTests -q` 与 `swift test -q` 均通过（57 tests, 0 failed）。

## 按键详情页 xcdocs 规范化（02:36）
- 用户要求“再调整，用规范指南 `$xcdocs`”。
- 查询并采用：
  - `Form`（数据录入容器）
  - `HSplitView`（可调整分栏）
  - `confirmationDialog`（确认动作）
- 实施：
  - 主内容从固定 `HStack` 改为 `HSplitView`（左栏 min/ideal/max 宽度）。
  - 取消确认在 macOS 12+ 使用 `confirmationDialog`，macOS 11 继续回退 `Alert`。
  - 右侧 `Form` 保持标签列与冒号风格。
- 验证：`swift test -q` 通过（57 tests, 0 failed）。

## Preview JIT 缺失 resource_bundle_accessor.o 修复（10:25）
- 用户反馈 Preview 报错：`CouldNotLoadInputObjectFile`，无法加载 `resource_bundle_accessor.o`。
- 关键处理：
  - 将 `Package.swift` 的 `EnjoyableKit` product 从显式静态库改为默认库类型。
  - 执行 `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme EnjoyableKit -configuration Debug clean build`。
  - 已确认文件存在：`.../Objects-normal/arm64/resource_bundle_accessor.o`。
- 同步优化：按 `swiftui-view-refactor` 规范重排 `AppleKeyMappingEditorSheet.swift`，采用 `private extension + MARK` 分区。
- 验证：`swift test -q` 通过（57 tests, 0 failed），`xcodebuild clean build` 成功。

## Sidebar 语义纠正（10:33）
- 用户指出左栏未按 `xcdocs` 规范设计。
- 已纠正：左栏改为 `List + Section + .sidebar`，替代 `ScrollView + GroupBox` 卡片式结构。
- 依据：`SidebarListStyle` / `ListStyle.sidebar` / `List Styling` 文档。
- 验证：`swift test -q` 与 `xcodebuild ... EnjoyableKit build` 均通过。

## 详情页与主界面样式统一（13:10）
- 用户确认继续收敛“按键详情页 vs 主界面”样式一致性。
- 采用 TDD：
  - 先在 `AppleNativeDesignTests` 新增 `inputFieldBackgroundOpacity` 断言并触发红灯；
  - 再新增统一 token 与修饰器实现转绿。
- 实施：
  - `AppleNativeDesignMetrics` 新增 `inputFieldBackgroundOpacity = 0.7`。
  - `EnjoyableRootStyling.swift` 新增 `nativeInputFieldBackground()`，统一输入底板样式。
  - `EnjoyableRootView.swift` 与 `AppleKeyMappingEditorSheet.swift` 的键位输入/宏步骤输入全部改为统一修饰器，消除重复实现。
- 验证：
  - `swift test -q` 通过（57 tests, 0 failed）。
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme EnjoyableKit -configuration Debug build` 成功。

## 映射列表交互调整（13:20）
- 用户要求：列表模式移除操作按钮，全部改为右键菜单，并高亮当前选中账号（映射）。
- 实施：
  - `EnjoyableMappingSidebarView` 列表模式不再显示底部增删移动作按钮；
  - 增删移统一收敛到右键菜单；
  - 右键操作前先切换选中行，保证操作作用于右键目标；
  - 当前选中/激活映射行增加高亮背景。
- TDD：新增 `MappingSidebarContextActionStateTests` 覆盖动作可用性边界。
- 验证：`swift test -q` 通过（61 tests, 0 failed），`xcodebuild ... EnjoyableKit build` 成功。

## Source & Diagnostics Panel xcdocs 收敛（23:33）
- 用户要求：`sourceAndDiagnosticsPanel` 按 `$xcdocs` 指南改造。
- 关键决策：
  - 按 SwiftUI 文档语义，将侧栏列表显式设置为 `.listStyle(.sidebar)`，避免样式依赖容器默认推断。
  - 将诊断状态文案 key 与图标名下沉到 `KeyMappingEditorState`，减少视图内联分支，提升可维护性与测试可达性。
- xcdocs 依据：
  - `/documentation/SwiftUI/SidebarListStyle`
  - `/documentation/SwiftUI/ListStyle/sidebar`
  - `/documentation/SwiftUI/List#Styling-lists`
- TDD：
  - 先在 `KeyMappingEditorStateTests` 新增诊断状态映射断言并触发红灯（缺少状态属性）。
  - 补实现后转绿。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test -q` 全量通过（76 tests, 0 failed）。

## 设置面板改为主流侧边栏结构（14:08）
- 用户确认按 Prowl 风格推进：设置区域采用左侧导航/列表 + 右侧详情结构。
- 实施：
  - 映射设置切换为 `HSplitView` 双栏；
  - 左栏为 `List(.sidebar)` + Section；
  - 右栏展示当前选中映射详情与重命名；
  - 继续保持“列表模式无底部操作按钮，全部右键菜单操作”。
- 新增 `MappingSidebarSelectionState` 统一选中回退策略，并补充单测。
- 验证：`swift test -q`（65 tests, 0 failed）与 `xcodebuild ... EnjoyableKit build` 成功。

## 保存/取消内聚到右侧详情（14:21）
- 用户要求：保存、取消不要单独占一条，放到右侧详情面板内部。
- 已实现：
  - 右侧详情使用草稿输入 + 面板内 `取消/保存`；
  - 取消回滚草稿，不提交；
  - 保存/回车在草稿非空且有变化时提交重命名。
- 新增 `MappingRenamePanelState` 与对应测试，确保交互边界稳定。
- 验证：`swift test -q`（69 tests, 0 failed），`xcodebuild ... EnjoyableKit build` 成功。

## Inspector 重新设计（15:45）
- 用户要求按 `xcdocs` 对 inspector 重新设计。
- 方案：将 inspector 从“卡片纵向堆叠”改为“List + Section”结构化面板，按 `status / mappings / output` 分区。
- 实施要点：
  - 增加 `InspectorPanelSectionDescriptor` 统一角色到 section 元数据映射；
  - `EnjoyableRootView` 传入动态 `inspectorRoles`，按角色构建 section；
  - 维持现有卡片内容，仅重构容器层级与语义。
- TDD：新增 `InspectorPanelSectionDescriptorTests` 覆盖顺序与标题映射。
- 验证：`swift test -q`（71 tests, 0 failed），`xcodebuild ... EnjoyableKit build` 成功。

## AppleKeyMappingEditorSheet 分区拆文件（22:08）
- 用户要求将 `AppleKeyMappingEditorSheet` 各区域拆分到独立文件。
- 在不改变行为的前提下完成结构化拆分：
  - `AppleKeyMappingEditorSheet.swift`：保留主结构与弹窗入口。
  - `AppleKeyMappingEditorSheet+Subviews.swift`：承载界面区域（header/split/sidebar/editor/form）。
  - `AppleKeyMappingEditorSheet+State.swift`：承载视图派生状态。
  - `AppleKeyMappingEditorSheet+Actions.swift`：承载交互动作与 `Binding`。
  - `AppleKeyMappingEditorSheet+Preview.swift`：承载 Preview。
- 为支持跨文件扩展访问，将原文件中仅供扩展使用的成员改为类型内可访问级别（无行为变更）。
- 验证：`swift test` 全量通过（71 tests, 0 failed）。

## Inspector 对齐 Prowl 导航结构（23:08）
- 用户要求继续按 Prowl 设置页风格推进 inspector：左侧导航列表、右侧详情切换。
- 使用 `xcdocs` 校验并采用官方形态：
  - `NavigationSplitView.init(sidebar:detail:)`（双列语义）
  - `List.init(selection:content:)`（单选导航列表）
- 实施：
  - `EnjoyableInspectorPanelView` 从单列 section 列表升级为内部 `HSplitView` 双栏。
  - 左栏改为 `List(selection:) + .sidebar`，右栏展示当前选中模块标题与内容。
  - 新增 `InspectorPanelNavigationState` 统一处理选中回退（无效选中时回退首项）。
  - `RootInspectorRole` 改为 `Hashable`，满足 `List(selection:)` 选择绑定约束。
- TDD：
  - 新增 `InspectorPanelNavigationStateTests`，先红后绿。
- 验证：
  - `swift test --filter InspectorPanelNavigationStateTests -q` 通过。
  - `swift test -q` 通过（74 tests, 0 failed）。
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme EnjoyableKit -configuration Debug build` 成功。
