# 2026-03-20

- 重建后的 `Enjoyable/Enjoyable.xcodeproj` 已接入本地 Swift Package：`../Package.swift`，产品 `EnjoyableKit`。
- 为修复 `dyld: Library not loaded @rpath/EnjoyableKit.framework` 与 Team ID 签名冲突，已将 `EnjoyableKit` 产品从 `.dynamic` 改为 `.static`，避免运行时动态装载。
- 首页已从占位 UI 切换为 `EnjoyableRootView`，并增加测试环境降级渲染分支，避免单测宿主启动阶段触发运行时崩溃。
- 权限策略：将 App Target 的 `ENABLE_APP_SANDBOX` 从 `YES` 调整为 `NO`，用于恢复 HID/事件模拟链路可用性（与旧工程无沙盒约束策略保持一致）。
- 验证结果：`xcodebuild clean build` 成功，App 可正常拉起；`swift test`（Package 侧 33 个测试）全通过；`xcodebuild test` 仍受当前 scheme/test plan 与 UI 测试流程影响，需后续单独整理。
- `qmd update` 成功；`qmd embed` 默认路径触发 `ggml-metal` 断言，使用 `GGML_METAL=0 qmd embed` 已成功完成向量更新。

## AppIcon 重新补齐（19:16）
- 背景：`Enjoyable/Enjoyable/Assets.xcassets/AppIcon.appiconset` 一度仅剩 `AppIcon512x512.png`，其他尺寸未落地。
- 处理：重新生成 16/32/128/256/512 的 1x/2x 图标文件，并为 `Contents.json` 全部槽位补上 `filename`。
- 验证：`xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -configuration Debug -destination 'platform=macOS' build` 通过。

## Apple 原生 UI/UX 全量重构（19:52）
- 用户要求按 Apple 交互语言重做界面，并指定 `ui-ux-pro-max`、`frontend-ui-ux-engineer`、`swiftui-ui-patterns`、`swiftui-liquid-glass`、`swiftui-view-refactor` 组合执行。
- 执行路径采用 BDD + TDD：先新增 `AppleNativeDesignTests`（红灯），再实现 `AppleNativeDesignMetrics` 与新根视图（绿灯）。
- 新增 `Sources/EnjoyableKit/SwiftUI/EnjoyableRootView.swift`：
  - 信息架构重建为 Sidebar + Detail；
  - `macOS 13+` 使用 `NavigationSplitView`，低版本回退 `NavigationView`；
  - 保留映射管理、实时输入、手柄布局、输出编辑、按键映射编辑全路径。
- 兼容决策：包最小平台为 `macOS 11`，已主动规避 `foregroundStyle`、`Material` 等高版本 API 强依赖；Liquid Glass 仅在条件满足时启用。
- 运行时调整：`DashboardStatusKind.icon` 语义化更新；`KeyCodeField` 放宽可见性；旧根视图命名调整以保留历史实现。
- 本次验证结果：
  - `swift test` 通过（37 tests, 0 failed）
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable ... build` 通过
  - `xcodebuild ... test -only-testing:EnjoyableTests` 通过

## SF Symbols 兼容修复（19:56）
- 修复运行日志中的缺失符号：
  - `l2.rectangle.roundedbottom.fill` -> `chevron.left.circle.fill`
  - `r2.rectangle.roundedbottom.fill` -> `chevron.right.circle.fill`
  - `mouse` -> `cursorarrow.click`
- 新增 `SFSymbolCompatibilityTests` 先红灯后绿灯，防止回归。
- 回归结果：`swift test` 与 Xcode `build` 均通过。

## 稳定回退开关（20:56）
- 应用户反馈“新版体验不可接受”，默认首页渲染切回旧根视图。
- 新版 UI 不删除，改为显式实验开关开启后才启用 `EnjoyableRootView`。
- 补充 `EnjoyableTests` 两条测试：默认旧版、实验开关启用 redesigned。
- 验证通过：`swift test -q`、`xcodebuild ... test -only-testing:EnjoyableTests`。

## UI 重排第 1 步（21:55）
- 需求聚焦为“先抛开最终视觉，仅梳理可用功能的信息架构”。
- 采用 TDD：先在 `AppleNativeDesignTests` 增加主区/检查器宽度 token 断言（红灯），再实现（绿灯）。
- `EnjoyableRootView.detail` 已从单列改为 `HSplitView` 双栏：
  - 主工作区：`controllerCard` + `liveAxisCard`
  - 检查器：`statusCard` + `outputEditorCard`
- 新增布局常量：`mainMinWidth=560`、`inspectorIdealWidth=340`、`inspectorMaxWidth=420`。
- 验证通过：
  - `swift test`（39 tests, 0 failed）
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## Sidebar 语义修正（22:16）
- 用户指出“侧边栏处理不对”，按 app 文档回看后确认问题在于之前侧栏被做成了自定义卡片式容器，不够原生。
- 已将 Sidebar 改为 `List(selection:)` + `.listStyle(.sidebar)`，并移除 `nativeSidebarSurface` 样式包裹与列表固定高度限制。
- 映射列表、映射操作、全局开关与权限引导整理为标准 Sidebar Section。
- 回归通过：
  - `swift test`（39 tests, 0 failed）
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## 默认入口切换到 NavigationSplitView 主界面（22:19）
- 用户明确要求“直接换”，首页默认入口已切换回 `EnjoyableRootView`（即 `NavigationSplitView` 主界面）。
- 回退策略由“实验开关启用新版”调整为“显式回退到旧版”（仅需要临时回滚时使用）。
- 测试环境 fallback 仍保留，避免测试宿主渲染真实根视图导致不稳定。
- 验证通过：
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`
  - `swift test --filter AppleNativeDesignTests`

## 侧边栏头部去重（22:29）
- 用户指出“导航区应在侧边栏语义内”，确认问题是侧边栏内部重复放了应用标题区，导致视觉上像双层导航。
- 已删除 Sidebar 内部 `app_title/app_subtitle` 头部 section，保留系统 `NavigationSplitView` 工具栏作为唯一导航头。
- 回归通过：
  - `swift test --filter AppleNativeDesignTests`
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## 侧边栏独立背景色（22:33）
- 用户要求“侧边栏要有自己的背景色”。
- 采用 TDD：先为 `sidebarBackgroundOpacity` 加红灯断言，再实现 `nativeSidebarBackground()`。
- 在 `macOS 13+` 下启用 `.scrollContentBackground(.hidden)`，确保自定义背景不会被系统列表底色覆盖。
- 回归通过：
  - `swift test --filter AppleNativeDesignTests`
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## 侧边栏去卡片化（22:38）
- 用户指出侧边栏仍有卡片观感，根因是 `List` 中混入 `TextField/Toggle/按钮组` 等表单控件。
- 已将侧边栏重构为两段：
  - 上方：`List(.sidebar)` 仅显示映射导航项
  - 下方：独立工具区承载重命名、增删/排序、模拟事件与权限提示
- 新增设计 token：`sidebarToolsSpacing=10`，并补测试门禁。
- 回归通过：
  - `swift test --filter AppleNativeDesignTests`
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## 侧边栏去卡片化 + 状态栏同色（22:42）
- 用户要求“侧边栏移除卡片样式，状态栏和侧边栏背景色同一”。
- 侧边栏进一步简化：`List` 使用纯导航行，映射标题移到 `List` 外部，降低 section 壳层带来的卡片观感。
- 状态栏改为 `nativeStatusBar`，背景色复用 `sidebarBackground`，并新增 `statusBarBackgroundOpacity=0.84` 门禁。
- 回归通过：
  - `swift test --filter AppleNativeDesignTests`
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests`

## 设备运行区迁移到右侧检查器（23:17）
- 用户要求分工调整：Sidebar 只保留“映射导航 + 映射工具”，设备列表与“模拟事件”开关迁移到右侧检查器。
- 按 TDD 执行：
  - 先新增 `InspectorRuntimeControlStateTests`（红灯：缺少 `InspectorRuntimeControlState`）。
  - 再实现 `InspectorRuntimeControlState` 与 `EnjoyableDeviceRuntimeCardView`（绿灯）。
- 关键实现：
  - `EnjoyableMappingSidebarView` 移除模拟事件与辅助功能权限引导，仅保留重命名、增删、排序。
  - `EnjoyableInspectorPanelView` 扩展为三段：状态卡、设备运行卡（设备树 + 模拟事件 + 权限引导）、输出编辑卡。
  - 设备树仍支持叶子节点右键“按键详情”入口，保持原操作路径。
- 验证结果：
  - `swift test` 通过（40 tests, 0 failed）。

## 侧栏/检查器职责再次交换（23:24）
- 用户最新要求：`侧边栏=设备与运行`，`右侧检查器=映射导航+映射工具`。
- 按 TDD 执行：
  - 新增 `RootPanelLayoutTests` 先红灯（`RootPanelLayout` 未定义）。
  - 实现 `RootPanelLayout.current`（sidebar: deviceRuntime；inspector: status/mappingManager/outputEditor）后绿灯。
- 关键实现：
  - 新增 `EnjoyableDeviceRuntimeSidebarView`，把设备树、模拟事件 Toggle、权限提示和跳转按钮放回 Sidebar。
  - 检查器中段改为映射管理卡片，内部承载 `EnjoyableMappingSidebarView`（映射列表 + 重命名 + 增删 + 上下移动）。
  - `EnjoyableMappingSidebarView` 增加可配置外观参数，支持在 Sidebar 与 Inspector 两处复用。
  - 新增文案 key：`device_runtime_title`（中/英）。
- 验证结果：
  - `swift test --filter RootPanelLayoutTests` 通过。
  - `swift test` 通过（42 tests, 0 failed）。

## Linker 符号兼容修复（23:30）
- 问题：Xcode 链接阶段报 `Undefined symbol: EnjoyableMappingSidebarView.init(...)`，外部模块仍引用旧构造器签名。
- 处理：在 `EnjoyableMappingSidebarView` 新增“旧签名兼容 init”，内部转调新构造器，保持 API 二进制兼容。
- 验证：
  - `swift test` 通过（42 tests, 0 failed）
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' build` 通过。

## 移除兼容层，仅保留新构造器（23:31）
- 用户明确要求“不兼容，直接改成新的”。
- 已删除 `EnjoyableMappingSidebarView` 的旧签名兼容 `init`，只保留新签名（`showsTitle/usesSidebarBackground/usesSidebarListStyle`）。
- 验证：
  - `swift test` 通过（42 tests, 0 failed）
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable clean build -destination 'platform=macOS'` 通过。

## 侧栏树形节点动效与图标修正（23:48）
- 用户反馈：设备侧栏子节点展开有“飞进来”动效，且图标语义不正确。
- 修正：
  - 设备树 `List` 关闭动画事务：`.transaction { animation = nil }`。
  - 引入 `DeviceTreeNodeStyle.symbolName(for:)`，按语义映射设备/分组/叶子图标。
  - 设备侧栏预览补充真实设备树假数据，便于直接目测验证。
- 测试与验证：
  - 新增 `DeviceTreeNodeStyleTests`（4 条）全部通过。
  - `swift test` 全量通过（46 tests, 0 failed）。

## 移除旧版 UI 分支（23:53）
- 用户要求“移除旧版全部内容”。
- 执行：
  - `ContentView` 删除 `HomeRootVariant/homeRootVariant` 及旧版分支选择逻辑，非测试场景统一进入 `EnjoyableRootView`。
  - 删除 `EnjoyableRuntime.swift` 中 `VisualEffectView`、旧根视图、旧 `KeyMappingEditorSheet` 等旧版 UI 代码段。
  - 更新 `EnjoyableTests`：移除旧版开关语义，验证入口在非测试环境下始终为 `enjoyableRoot`。
  - 更新 `docs-dev/features` 与 `docs-dev/dev`，移除旧版回退开关描述。
- 验证：
  - 红灯：`xcodebuild ... test -only-testing:EnjoyableTests`（预期失败，旧版开关断言不再成立）。
  - 绿灯：`swift test` 通过；`xcodebuild ... test -only-testing:EnjoyableTests` 通过。

## 旧版术语残留清理（23:58）
- 在 `Sources/EnjoyableKit/Classes/Output/NJOutputKeyPress.swift` 将 `legacyKey` 重命名为 `serializedKey`。
- 在 `Tests/EnjoyableKitTests/KeyPressThresholdTests.swift` 将测试名与样例变量改为“缺少阈值字段”语义，去除旧版术语。
- 清理 `memory/2026-03-20.md` 中旧版英文术语与旧环境变量名描述，统一为“旧版/回退开关”中文表述。
- 检索验证：`rg -n --hidden --glob '!.git' -i 'legacy|ENJOYABLE_UI_LEGACY|ENJOYABLE_UI_EXPERIMENTAL'` 无结果。
- 回归验证：
  - `swift test` 通过（46 tests, 0 failed）。
  - `xcodebuild -project Enjoyable/Enjoyable.xcodeproj -scheme Enjoyable -destination 'platform=macOS' test -only-testing:EnjoyableTests` 通过。
