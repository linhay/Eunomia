# 2026-03-23

## Enjoyable TCA 继续简化（01:30）
- 按 `$composable-architecture` 持续收敛 `EnjoyableRootView` 样板代码：
  - 增加 `sendView(_:)` 统一 `store.send(.view(...))`。
  - 增加 `binding(get:set:)` 重载，索引绑定改复用 helper。
  - `onAppear` / 多处按钮回调统一走 `sendView`。
- 结论：本轮为纯结构重构，目标是降低重复模板与维护成本，行为不变。
- 验证：
  - `swift test --filter EnjoyableRootFeatureTests` 触发 Swift 编译器断言失败。
  - `swift test --filter EnjoyableStoreTests` 同样触发断言失败。
  - 失败点位于 TCA 依赖编译阶段（`Alert.swift`），错误为 `LocalDiscriminator is set multiple times`。
- 风险：当前 shell Swift 工具链下无法完成本轮测试门禁验证，需要在已知可用的 Xcode 默认工具链环境再次回归。

## Enjoyable TCA 继续简化（01:40）
- `EnjoyableRootFeature` 引入 keyPath 版 runtime helper：
  - `runAndRefresh(keyPath:)`
  - `runAndRefresh(_:keyPath:)`
- 多个 action 分支改为 keyPath 调度，降低 switch 内重复模板。
- `EnjoyableRootView` 增加 `send(_:)`，统一非 view action 发送入口。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 再次被 Swift 6.2 shell 工具链断言阻塞（`LocalDiscriminator is set multiple times`，TCA `Alert.swift`）。

## Enjoyable TCA 继续简化（01:50）
- `openKeyMappingEditorForInput` / `commitRename` 迁移到 keyPath 版 `runAndRefresh` 调度。
- 新增二参 helper：`runAndRefresh(_:_:keyPath:)`，统一双参数 runtime 调用模板。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍因 Swift 6.2 shell 工具链编译 TCA (`Alert.swift`) 断言失败。

## Enjoyable TCA 继续简化（02:00）
- 新增 `updateDraftFloat`，收敛 `Double -> Float` 草稿写入样板。
- 新增 `updateKeySequenceStep`，收敛键序列步骤的索引检查与读取样板。
- `setKeySequenceKey / setKeySequenceDelay` 已迁移到统一 helper。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍被 Swift 6.2 shell 工具链断言阻塞；本次堆栈指向 TCA `Sheet.swift`（同类 `LocalDiscriminator is set multiple times`）。

## Enjoyable TCA 继续简化（02:10）
- 新增 `updateDraftForKeyCode`，将 `setKeyCode` 的“主键与序列首步联动”逻辑从 `switch` 分支抽离。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍因 Swift 6.2 shell 工具链断言失败；本次定位 TCA `ConfirmationDialog.swift`（同类 `LocalDiscriminator is set multiple times`）。

## Enjoyable TCA 继续简化（02:20）
- 新增 `replaceKeySequenceStep`，统一 `NJKeySequenceStep` 替换写法。
- `setKeySequenceKey / setKeySequenceDelay / updateDraftForKeyCode` 已切到统一 helper。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍被 Swift 6.2 shell 工具链断言阻塞；本次定位 TCA `Popover.swift`（同类 `LocalDiscriminator is set multiple times`）。

## Enjoyable TCA 继续简化（02:30）
- 按 TDD 新增 3 个 `EnjoyableRootFeatureTests` 场景：
  - keySequence delay 负值钳制为 0。
  - setKeyCode 同步首个序列步骤 keyCode 且保留 delay。
  - setKeyCode 在空序列时补首个步骤。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍被 Swift 6.2 shell 工具链断言阻塞，本次再次落到 TCA `ConfirmationDialog.swift`。

## Enjoyable TCA 继续简化（02:40）
- 测试新增 helper：`makeStoreWithDraftSnapshot`，减少重复建店样板。
- 新增 2 个边界场景：
  - keySequence delay 越界索引不应改动 draft。
  - set 空 keyCode 时不应自动创建序列首步。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍被 Swift 6.2 shell 工具链断言阻塞（本次定位 `Popover.swift`）。

## Enjoyable TCA 继续简化（02:55）
- 按“Main + Logic + Preview”合并 `AppleKeyMappingEditorSheet` 文件结构：
  - 新增 `AppleKeyMappingEditorSheet+Logic.swift`。
  - 删除 `+Actions/+State/+Subviews` 三个文件。
- 修复确认弹窗文案 key 污染：恢复为 `discard_changes_message`。
- 验证：`swift test --filter EnjoyableRootFeatureTests` 仍被 Swift 6.2 shell 工具链断言阻塞（本次定位 `Popover.swift`）。

## Enjoyable Trigger Events 组合键输入改造（03:35）
- 用户确认希望本行直接监听组合键，不再使用 type 选择和行内 `+`。
- 已将 Trigger Events keys 列改为单输入框 `KeyComboField`：一次捕获并保存整组 keys。
- 显示层面移除输入框内 `+` 分隔符，改为同一输入框连续展示（空格分隔）。
- 风险：本地测试依然受 Swift 6.2 + TCA 编译器断言阻塞，无法完成完整自动化回归。

## Enjoyable KeyComboField 编译修复（03:55）
- 已修复 `KeyComboField` 的 `kVK_*` 未定义与 `init` 歧义问题。
- 关键动作：`import Carbon` + 统一 `UInt16` 键码流。
- 当前风险不变：整体构建仍受 Swift 6.2 + TCA 编译器断言影响。

## Save 关闭行为修复（04:20）
- 用户反馈保存无效且页面不关闭。
- 根因：保存路径会写 defaults，但未清空 `keyMappingEditorState`，sheet 绑定条件持续为非空。
- 修复：`applyKeyMappingEditorState` 末尾设置 `keyMappingEditorState = nil`。
