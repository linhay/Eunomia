# 2026-03-24

## 一次性内部命名重构完成
- 决策：按“直接一次性重构”执行，将代码目录内 `Enjoyable*` 内部符号全部切换为 `Eunomia*`。
- 行动：
  - SwiftUI/TCA 核心类型：`EnjoyableRootView/Feature/RuntimeSnapshot/RuntimeClient/Store` 全量改名。
  - 相关 UI 组件、预览容器、测试类与测试文件名同步改名。
  - SwiftUI 文件名从 `Enjoyable*.swift` 批量迁移为 `Eunomia*.swift`。
  - 清理旧目录残留：移除根目录下遗留 `Enjoyable/` 临时目录。
- 兼容与一致性：
  - 通知与映射常量字符串命名改为 `com.yukkurigames.Eunomia.*`。
  - 映射解析错误域由 `Enjoyable` 改为 `Eunomia`。
- 验证：
  - `swift test -q`：131 tests 全通过。
  - `xcodebuild -project Eunomia/Eunomia.xcodeproj -scheme Eunomia -destination 'platform=macOS' build`：`BUILD SUCCEEDED`。
  - `rg -n "Enjoyable" Sources Tests Eunomia`：无结果。

## 文档命名收口（dev/features）
- 行动：批量将 `docs-linhay/dev` 与 `docs-linhay/features` 的技术术语从 `Enjoyable*` 收口到 `Eunomia*`。
- 例外：保留两份迁移说明文档中的 `Enjoyable` 历史映射描述（用于说明 old -> new 关系）。
- 验证：`rg -n "Enjoyable" docs-linhay/dev docs-linhay/features` 仅命中迁移说明文档。

## GitHub Actions 自动打包接入
- 决策：按“公开仓库自动打包”接入 GitHub Actions，采用 macOS runner 直接构建 `Eunomia.app`。
- 行动：
  - 新增 workflow：`.github/workflows/macos-package.yml`。
  - 触发策略：`push`、`pull_request`、`workflow_dispatch`，其中 tag `v*` 额外发布 Release 资产。
  - 流水线步骤：`swift test` -> `xcodebuild`（`CODE_SIGNING_ALLOWED=NO`）-> `ditto` 打 zip -> 上传 artifact。
  - 发布策略：tag 触发时用 `softprops/action-gh-release` 附加 zip。
- 风险与约束：
  - 当前为未签名构建产物，适用于测试/验收下载，不可直接用于 notarization 分发。
  - 若后续需要正式发行包，应增加签名与公证流程。

## Releases 未产出 app 的修复
- 现象：`macOS Package` workflow 在 `Swift Tests` 阶段失败，导致后续构建与 release 上传步骤未执行。
- 根因：
  - GitHub runner 使用 `macos-14`（Xcode 15.4），与依赖图中部分包版本不兼容，`swift test` 编译失败。
  - workflow 仅监听 tag push 上传 release 资产，未覆盖 GitHub UI 手动发布 release 的事件。
- 修复：
  - runner 升级到 `macos-15`（更新 Xcode toolchain）。
  - 新增 `release.published` 触发。
  - 将 release 上传拆分为两个步骤：`tag push` 发布、`published release` 补充上传。

## 许可整理与合规落位
- 决策：采用“主许可证 + 第三方声明”双文件结构，根目录统一落位 `LICENSE` 与 `THIRD_PARTY_NOTICES`。
- 行动：
  - 新增 `LICENSE`（MIT，项目许可入口）。
  - 重整 `THIRD_PARTY_NOTICES`：保留上游 MIT 署名全文与 joystick icon 公有领域声明。
  - 根据 `.build/checkouts/*/LICENSE*` 实测汇总 SwiftPM 依赖许可（MIT + Apache-2.0）。
  - 更新 `README.md` 的 License 章节，指向上述两类文件。
- 风险结论：发布二进制时应随包附带 `THIRD_PARTY_NOTICES` 与依赖许可全文，避免 Apache-2.0 文本遗漏。
- 补充决策：主许可证版权抬头从 `Eunomia Contributors` 调整为 `linhay`，已更新 `LICENSE`。
