# 2026-03-22

## 移除 AppleNativeCard（01:06）
- 用户要求：移除 `AppleNativeCard`。
- 现状确认：`AppleNativeCard` 定义已缺失，但 `EunomiaRootView` / `EunomiaRootComponents` 仍有 3 处调用，导致 `swift test` 编译失败。
- 关键决策：
  - 不恢复旧组件，改为引入通用容器 `NativePanelCard`，将“标题行 + 内容 + surface 样式”收敛到一个可复用视图。
  - 保留既有 `RootCardSurfacePolicy` 与 `.nativeCardSurface(...)`，确保视觉策略不漂移。
- 实施：
  - 新增 `NativePanelCard`。
  - 替换 `controllerCard`、`outputEditorCard`、`EunomiaLiveAxisCardView` 的 `AppleNativeCard` 调用。
  - 新增测试 `testNativePanelCardCanBeConstructedWithSolidSurface`。
- 验证：
  - `swift test --filter AppleNativeDesignTests -q` 通过。
  - `swift test -q` 全量通过（77 tests, 0 failed）。

## AppleKeyMappingEditorSheet 两张语义卡 + 组合键双轨（01:28）
- 用户要求按既定方案直接实现：右侧改为“按钮状态卡 + 触发事件卡”，并支持单键/组合键双轨步骤。
- 关键决策：
  - 步骤模型升级为 `keys + delay`，保留 `keyCode` 兼容访问器降低改造风险。
  - 触发行为兼容旧语义：单步单键且无延时继续采用按下/松开；其余步骤走延时事件序列。
  - 序列化执行“一次性迁移”：读取兼容旧 `key/sequence`，保存仅写新 `steps`。
- 实施：
  - `NJOutputKeyPress`：新步骤结构、兼容读取、统一写新结构、组合键触发顺序。
  - `AppleKeyMappingEditorSheet`：右侧改两卡；增加步骤类型切换、组合键字段增删、步骤上下移动。
  - `KeyMappingEditorState`/`OutputDraft`：主键推导与有效性判定改为从 `keys` 推导。
  - 本地化新增两卡与双轨编辑文案（中英）。
- 测试：
  - 新增/更新 `KeyPressThresholdTests`、`KeyMappingEditorStateTests`、`OutputDraftTests`。
  - `swift test -q` 全量通过（81 tests, 0 failed）。

## Trigger Events 表格化（01:35）
- 用户追加要求：`trigger events` 改为 table 模式。
- 实施：在触发事件卡内改为表格化 Grid 布局，列为“步骤/类型/按键/延时/顺序”，并保留单键/组合键双轨与步骤操作。
- 说明：直接使用 SwiftUI `Table` 在当前表达式复杂度下触发编译器 type-check 超时，改为表格化 Grid 以保持交互能力与稳定编译。
- 验证：`swift test -q` 通过（81 tests, 0 failed）。

## Trigger Events 行样式重思考（01:43）
- 用户要求结合 `$xcdocs` + `$ui-ux-pro-max` 重新思考样式与设计。
- 依据：
  - `xcdocs`：`Grid` / `GridRow` 的二维对齐语义，行视觉由外层容器承载。
  - `ui-ux-pro-max`：高优先级关注对比度、点击目标尺寸、非颜色单一提示、可访问标签。
- 实施：
  - 表头升级为独立容器（底色+描边）；
  - 每行升级为统一“行卡片”（圆角、描边、斑马底）；
  - 步骤编号改为圆形徽标；行左侧新增类型强调条（single/combo）；
  - 上下移动按钮改为 bordered，并新增无障碍标签文案。
- 验证：
  - `swift test --filter LocalizationTests -q` 通过。
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test -q` 全量通过（81 tests, 0 failed）。

## Trigger Events 分组式编辑器重构（01:58）
- 用户进一步要求“重新设计”，不只是调整每一行样式。
- 关键决策：
  - 放弃继续强化 table/row 心智，转为“每个步骤一个编辑分组”的任务型设计。
  - 触发步骤更适合录入和修改，而不是做横向数据比较，因此采用 `GroupBox` 承载完整步骤编辑。
- 依据：
  - `xcdocs`：`GroupBox` 与“为数据录入挑选容器”的文档更契合当前场景。
  - `ui-ux-pro-max`：强调任务分组、摘要扫描、稳定操作位置和更清晰的信息层级。
- 实施：
  - 每个步骤头部展示编号、类型标签、摘要和上下移动按钮；
  - 分组内部按“类型 -> 延时 -> 按键”顺序组织输入；
  - 保留组合键双轨、增删按键和顺序调整能力。
- 验证：
  - `swift test --filter LocalizationTests -q` 通过。
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。

## Trigger Events 头部摘要增强（10:20）
- 用户要求继续推进重设计。
- 关键决策：
  - 不额外增加结构复杂度，优先提升步骤头部的信息密度。
  - 复用 `NJKeyInputField.displayName(forKeyCode:)` 生成真实按键名，避免新建重复映射逻辑。
- 实施：
  - 将步骤摘要逻辑下沉到 `KeyMappingEditorState`；
  - 单键/组合键摘要改为显示真实按键名与延时；
  - 测试断言改为跟随 `L10n` 模板，避免被本地化环境影响。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test --filter LocalizationTests -q` 通过。

## Trigger Events 折叠与长摘要控制（10:26）
- 用户继续推进同一轮 UI 重构。
- 关键决策：
  - 长宏序列优先解决纵向占用问题，因此给每个步骤增加折叠能力。
  - 头部摘要在多组合键场景下自动截断到有限键名，避免头部过长破坏层级。
- 实施：
  - 新增步骤折叠状态，支持单步展开/折叠与“展开全部 / 折叠全部”；
  - 步骤移动时同步交换折叠状态，新增步骤默认展开，删除步骤后自动归一化状态；
  - 摘要逻辑支持 `maximumVisibleKeyNames`，超过部分以 `+ ...` 表示。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test --filter LocalizationTests -q` 通过。

## Trigger Events badge 化摘要（10:42）
- 用户继续要求推进设计完成度。
- 关键决策：
  - 不再用单行文字承担全部摘要信息，改成更接近 inspector / token editor 的 badge 视觉。
  - 状态层提供“可见键名 + 隐藏数量”出口，视图只负责呈现。
- 实施：
  - 步骤头部改为按键 token、`+N`、延时 badge 组合；
  - 组合键编辑区顶部增加 token 预览；
  - 新增状态测试覆盖可见 token 与隐藏数量推导。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test --filter LocalizationTests -q` 通过。

## Trigger Events badge 层级统一（11:02）
- 用户同意继续推进 badge 层级和 token editor 风格。
- 关键决策：
  - 主键、延时、隐藏数量、空态 badge 采用不同强调等级，而不是同一视觉重量。
  - 单键编辑区也补 token 预览，避免单键和组合键使用两套不同语法。
- 实施：
  - 在 `AppleKeyMappingEditorSheet+Subviews.swift` 内引入 `StepBadgeKind`；
  - 头部和编辑区 token 统一复用 `compactStepBadge`；
  - 单键编辑区增加与组合键一致的 token 预览。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test -q` 全量通过（85 tests, 0 failed）。

## Trigger Events 主动作锚点（19:25）
- 用户继续推进视觉层级。
- 关键决策：
  - 将每一步的第一个有效 key 明确视为 primary action，并在头部与编辑区都重点强调。
  - 其余 key 退为 secondary，隐藏数量继续用弱化 badge 表示。
- 实施：
  - `KeyMappingEditorState` 新增 primary/secondary key 名拆分接口；
  - `AppleKeyMappingEditorSheet+Subviews.swift` 采用 `primaryAnchor` 与 `secondary` badge 风格。
- 验证：
  - `swift test --filter KeyMappingEditorStateTests -q` 通过。
  - `swift test -q` 全量通过（86 tests, 0 failed）。

## Trigger Events Inspector 两栏与当前步骤高亮（19:28）
- 用户继续推进“更像 inspector”。
- 关键决策：
  - 展开区不再只是纵向堆叠字段，改为左元信息、右按键编辑的两栏结构。
  - 增加当前步骤选中态，并保持索引变化时选中值可归一化。
- 实施：
  - `AppleKeyMappingEditorSheet` 新增 `selectedStepIndex`；
  - `AppleKeyMappingEditorSheet+State.swift` 提供选中索引归一化与查询；
  - `AppleKeyMappingEditorSheet+Subviews.swift` 增加两栏展开布局和当前步骤高亮描边；
  - 新增 `testStepSelectionNormalizesIntoBounds`。
- 验证：
  - `swift test --filter AppleNativeDesignTests -q` 通过。
  - `swift test -q` 全量通过（87 tests, 0 failed）。

## TCA 集成阻塞与收敛（23:20）
- 用户要求集成 `pointfreeco/swift-composable-architecture`，并提示 `onevcat/Prowl` 已使用 TCA。
- 关键结论：
  - 当前本地工具链 `swift-6.2-RELEASE` 下，只要 target 真实依赖 `ComposableArchitecture`，编译会触发断言崩溃：`LocalDiscriminator is set multiple times`。
  - 已验证 `1.25.2`、`1.23.1`（Prowl 同版本）、`0.59.0`、`main` 均可复现，问题更像编译器侧。
  - 额外最小包实验显示：即便设置 `swiftLanguageVersions: [.v5]` 仍复现；崩溃稳定指向 TCA `SwiftUI/Popover.swift` 闭包分析阶段。
- 收敛动作：
  - 删除实验残留 `Sources/TCAIntegrationSpike/`，避免悬空代码。
  - 新增阻塞文档 `docs-linhay/dev/tca-integration-blocked-by-swift-toolchain-2026-03-22.md`。
  - 更新 `docs-linhay/features/tca-integration-2026-03-22.md` 写回当前状态。
- 风险与策略：
  - 短期保持“已声明依赖、target 暂不引用”以保证主线可构建；
  - 后续需切换/对齐可用 Xcode 工具链再恢复 TCA 落地验证。

## TCA 集成真正落地（23:27）
- 新发现：`XcodeDefault Swift 6.2.4` 能稳定编译 TCA（同样依赖与最小 Feature），而 shell 默认 `swift-6.2-RELEASE (+assertions)` 会崩。
- 实施：
  - `Package.swift`：`EunomiaKit` 与 `EunomiaKitTests` 都接入 `ComposableArchitecture` product。
  - 新增 `Sources/EunomiaKit/SwiftUI/KeyMappingEditorTCAFeature.swift`。
  - 新增 `Tests/EunomiaKitTests/TCAIntegrationTests.swift`（`TestStore` 验证 `setEnabled` / `setThreshold`）。
- 验证：
  - 红灯：新增测试先失败（缺 Feature）。
  - 绿灯：补 Feature 后，Xcode 默认工具链下目标测试与全量测试通过（88 tests, 0 failed）。
  - 风险：当前 shell 默认 `swift test -q` 仍因编译器断言崩溃，需显式使用 Xcode 默认工具链执行。

## Eunomia 根页面全量 TCA 改造（23:43）
- 用户要求：使用 `$composable-architecture` 做完整改造。
- 关键决策：
  - 采用“runtime 桥接 + 根 reducer 接管 UI 状态”的迁移路径，不直接重写底层 `NJInputController`。
  - 新增 `EunomiaRuntimeClient` 和 `EunomiaRuntimeSnapshot`，将原 `EunomiaStore` 能力通过 action/effect 接入 TCA。
  - `EunomiaRootView` 改为 `StoreOf<EunomiaRootFeature>` 驱动，保留现有 UI 行为与交互结构。
- 主要落地：
  - 新增 `Sources/EunomiaKit/SwiftUI/EunomiaRootFeature.swift`。
  - `EunomiaRuntime.swift` 增加 `snapshot()` / `snapshotStream()`。
  - `EunomiaRootView.swift` 完成从 `@StateObject` 到 TCA store 的切换。
  - 新增 `Tests/EunomiaKitTests/EunomiaRootFeatureTests.swift`（红灯 -> 绿灯）。
  - 文档新增：
    - `docs-linhay/features/tca-full-refactor-2026-03-22.md`
    - `docs-linhay/dev/tca-full-refactor-implementation-2026-03-22.md`
- 验证：
  - `swift test --filter EunomiaRootFeatureTests` 通过。
  - `swift test` 全量通过（90 tests, 0 failed）。
- 风险提示：
  - 新 TCA 类型当前保持模块内可见（internal），后续若要作为 SDK 对外 API，需要再设计稳定公开接口。

## Eunomia TCA 继续简化（23:57）
- 目标：在不改变行为的前提下，继续收敛 TCA 样板代码。
- 关键动作：
  - 在 `EunomiaRootFeature` 增加 `runAndRefresh` 统一副作用后刷新状态的模板。
  - 删除 `EunomiaRuntimeClient` 未使用的 `canConfigure` 注入项。
  - `updateDraft` 使用 `let draftToPersist`，修复 Swift 6 并发捕获告警风险。
  - `EunomiaRootFeatureTests` 抽出 `makeRuntime` 工厂并新增 `testDismissKeyMappingEditorClearsEditorState`。
- 验证：
  - `swift test --filter EunomiaRootFeatureTests` 通过（3 tests）。
  - `swift test` 全量通过（91 tests, 0 failed）。

## Eunomia TCA 继续简化（2026-03-23 00:01）
- 决策：将根页面首次状态来源收敛到 runtime stream，移除 `onAppear` 的重复 snapshot 拉取。
- 测试策略同步：`makeRuntime` 默认模拟“先发首帧再结束”的 updates 流，个别测试按需覆盖为空流。
- 验证：
  - `swift test --filter EunomiaRootFeatureTests` 通过（3 tests）。
  - `swift test` 全量通过（91 tests, 0 failed）。

## Eunomia TCA 继续简化（2026-03-23 01:15）
- 代码收敛：
  - 删除 `EunomiaRuntimeClient` 冗余显式 init。
  - `EunomiaRootFeature` 新增 `updateDraft(_:keyPath:value:)`，替换多处重复的简单字段写入。
- 设计取舍：
  - 复杂草稿变换（按键主键/序列步骤）保留 `mutate` 版本，避免泛化过度影响可读性。
- 验证：
  - `swift test --filter EunomiaRootFeatureTests` 通过。
  - `swift test` 全量通过（91 tests, 0 failed）。
