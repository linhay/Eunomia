import SwiftUI
import EnjoyableCore

struct ContentView: View {
    var body: some View {
        EnjoyableRootView()
            .frame(minWidth: 980, minHeight: 620)
    }
}

#Preview {
    ContentView()
}
